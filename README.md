# Weather data ETL pipeline.

This is a lightweight and scalable weather data pipeline. Local Cincinnati weather data is extracted via the Open-Meteo REST API, transformed using Pandas, and loaded into an SQL database using SQLite.
